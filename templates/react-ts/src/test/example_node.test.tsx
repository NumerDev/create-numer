// Its only an example and we don't need to use this docblock because
// by default environment is set to node
// @vitest-environment node
describe('Example', () => {
  it('Basic test', () => {
    const a = 5;
    expect(a).toBe(5);
  });
});
